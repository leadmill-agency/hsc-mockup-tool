import App from "@/components/App";

// Internal staff cockpit: projects list, full controls, pricing internals.
// The root URL is the public customer tool (PRD §7.0 open-tool entrance).
export default function StaffPage() {
  return <App />;
}
